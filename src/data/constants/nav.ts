import { UploadIcon } from "lucide-react";

import { NavItem } from "@/types";

export const NAV_ITEMS: NavItem[] = [
  {
    label: "Uploader",
    href: "/app/uploader",
    icon: UploadIcon
  },
]
